{

    "routes": [
        {
          "route": "/*",
          "methods": ["PUT", "POST", "PATCH", "DELETE"]
        }
        ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}
